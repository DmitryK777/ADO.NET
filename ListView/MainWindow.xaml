<Window x:Class="ListView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListView"
        mc:Ignorable="d"
        Title="WPF - ListView" Height="450" Width="800"
        FontSize ="22"
>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height ="35" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBox Name="txtEntry" Height="35" />

        <Grid Grid.Row="1">
            
        </Grid>

        <Button Name="BtnAdd" Grid.Column="1" Width="100" Content="Add" HorizontalAlignment="Left" Margin="5,0,0,0" Click="BtnAdd_Click" />
        <Button Name="BtnDelete" Grid.Column="1" Width="100" Content="Delete" HorizontalAlignment="Left" Margin="105,0,0,0" Click="BtnDelete_Click" />
        <Button Name="BtnClear" Grid.Column="1" Width="100" Content="Clear" HorizontalAlignment="Left" Margin="205,0,0,0" Click="BtnClear_Click" />

        <ListView Name="lvEntries" Grid.Row="1" Grid.ColumnSpan="2" />
    </Grid>
</Window>
